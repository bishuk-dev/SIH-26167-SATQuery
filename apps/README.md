# Applications

- `api/` is the FastAPI transport boundary. Route handlers and API schemas belong here; reusable domain logic belongs in `satquery/`.
- `web/` is the React/TypeScript imagery-first client boundary.

Framework scaffolding will be added with the first runnable vertical slice rather than maintained as unused boilerplate.
