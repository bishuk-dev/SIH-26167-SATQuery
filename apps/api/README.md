# API application

This directory will contain the FastAPI application, request/response schemas, routes, and application services. Phase 1 will introduce the first runnable API surface together with its tests.
